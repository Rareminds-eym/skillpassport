/**
 * @deprecated Supabase Realtime client has been replaced by SSE-based realtime.
 * Use `getSSEClient()` from `@/shared/api/sseRealtimeClient` instead.
 * The `RealtimeService` class also no longer relies on this module.
 */
export {};
