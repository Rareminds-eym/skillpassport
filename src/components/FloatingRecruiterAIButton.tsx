import React from 'react';

// FloatingRecruiterAIButton is currently disabled and renders nothing in the UI.
// To re-enable, restore the previous implementation.
const FloatingRecruiterAIButton: React.FC = () => {
  return null;
};

export default FloatingRecruiterAIButton;
