name = "assessment-api"
main = "src/index.ts"
compatibility_date = "2024-01-01"

[vars]
# These will be overridden by secrets in production

# Add secrets via: wrangler secret put SECRET_NAME
# Required secrets:
# - VITE_SUPABASE_URL
# - VITE_SUPABASE_ANON_KEY
# - CLAUDE_API_KEY (or VITE_CLAUDE_API_KEY)
