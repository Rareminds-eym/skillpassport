name = "user-api"
main = "src/index.ts"
compatibility_date = "2024-01-01"

[vars]
ENVIRONMENT = "production"
EMAIL_API_URL = "https://email-api.dark-mode-d021.workers.dev"

# Note: SUPABASE_URL, SUPABASE_ANON_KEY, and SUPABASE_SERVICE_ROLE_KEY
# should be set as secrets in Cloudflare dashboard for production
# For local development, create a .dev.vars file with these values
# 
# Email sending now uses Supabase Edge Function (send-email) with SMTP
# RESEND_API_KEY is no longer needed
